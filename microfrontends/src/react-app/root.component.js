import React from 'react';

export default class Root extends React.Component {
  render() {
    return (
      <div style={{marginTop: '100px'}}>
        This was rendered by react-app, which is written in React.
      </div>
    );
  }
}